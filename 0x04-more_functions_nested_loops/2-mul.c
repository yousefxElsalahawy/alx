#include "main.h"
/**
 * mul  - Write a function that multiplies two integers.
 * @a: first number
 * @b: second number
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
